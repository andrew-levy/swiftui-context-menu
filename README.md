# SwiftUI Context Menu

## Run the app

1. Install dependencies. Note there is a small patch to `expo-modules-core` so make sure that patch is applied

2. Prebuild the app

```bash
npx expo prebuild -p ios
```

3. Run it

```bash
npx expo run:ios
```

## Features

- Basic context menu
- Accessory view
- Preview
